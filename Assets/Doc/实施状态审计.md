# 实施状态审计 (v2 · 交叉验证后)

> 本文经过第二轮交叉验证修正,**已与 [架构设计-v2.md](架构设计-v2.md)、[实施计划.md](实施计划.md)、[测试用例.md](测试用例.md) 当前内容对齐**。
> 上一版的偏差已分两类处置:
> 1. **设计/计划/测试三本文档与实现不符** → 已就地修订源文档(本轮已完成,见 §2)。
> 2. **仍需进一步验证或新工作** → 收敛到下文 §4 "补充任务列表"。

---

## 1. 静态审计能给出的事实

| 维度 | 结论 |
|---|---|
| Runtime `using TreeEditor` / `using UnityEditor` | 0 命中(`grep` 全 `Assets/Scripts/**/*.cs`) |
| Runtime `Debug.LogError` | 0 命中;热路径日志均加 `[Conditional("UI_VERBOSE")]` |
| `UIPrefaHolder` 旧文件/旧 meta | 已清除,场景/Prefab 无残留引用 |
| `IUIData` 接口含 `FillWithMatrix` | ✅ ([UIMeshData.cs](../Scripts/UIDataRender/Mesh/UIMeshData.cs) L28) |
| `UIMeshDataX.FillToDrawData` / `FillWithMatrix` 索引重基 | 均使用 `geometry.indices[i] - VertexOffset + offset`,符合 [架构设计-v2.md §4.1/4.3](架构设计-v2.md) |
| `UIMeshDataX.FillToTriangleData` 索引循环 | 已修正为 `[IndicesOffset, IndicesOffset + IndicesCount)` |
| `GeometryBuffer.EnsureVertexCapacity` | 4 并行数组同步扩(uvs/vertex/drawVertex/colors);indices 独立 |
| `TextureSlotTable.Register` 超容量 | **追加槽位 + LogWarning,不再返回 -1**(实现层已稳定,文档本轮已对齐) |
| `UIPrefabManager` Facade 拆分 | 4 个协作者: `TextureSlotTable` / `HolderNotifier` / `OwnerRegistry` / `HolderLifecycle`(本轮已写入设计文档 §2.8) |
| `HolderBatchRenderer` | 已新增共享 Holder 渲染入口;`TestUIPrefabHolder` Slim/非 Slim 均走 `MergeBatcher.Plan` → `RenderBatch` → `MaterialBinder.Bind` |
| `PerfProbe` 运行路径 | `DataPrefabHolder.Fill` / `HolderBatchRenderer` 的 Merge/Fill/Draw 路径已接入 `ProfilerMarker`;新增 PlayMode CSV 落盘用例 |
| `BurstMergeJob` SoA 路径 | 已新增 `Jobs/MergeJobs.Burst.cs`;`UIDataRender.asmdef` 引用 `Unity.Burst`;PlayMode `TC-JOB-03` 覆盖输出等价 |
| `Enable8TexSlots` / `ExpandTo(7)` / `HUD_8_TEX_SLOTS` keyword | C# 与 Shader 双侧已实现([UIE-AtlasBlit.shader](../Resources/Shader/UIE-AtlasBlit.shader) 含 `_MainTex0..7` 与分支) |
| Coverlet + CI | `Packages/manifest.json` 已含 `com.unity.testtools.codecoverage`;`.github/workflows/unity-tests.yml` 已存在 |
| EditMode 测试集 | 17 文件全在 `Assets/Tests/EditMode/`;TC-MX 等价性断言并入 `TestUIMeshDataX.cs`(测试用例.md 已同步,非问题) |
| PlayMode asmdef | `Assets/Tests/PlayMode/TestUIDataRender.PlayMode.asmdef` 存在;已移除 `UnityEditor.TestRunner` 引用与 Editor-only 平台限制,由 PlayMode runner 识别 |

---

## 2. 本轮已就地修订的源文档(无需新工作)

| 修订项 | 落地位置 | 起因 |
|---|---|---|
| `TextureSlotTable.Register` 超容量语义改为 "追加 + Warn",删除 "return -1" 草案 | [架构设计-v2.md §2.4](架构设计-v2.md) · [架构设计-v2.md §4.4 伪代码](架构设计-v2.md) · [实施计划.md TASK-10](实施计划.md) · [测试用例.md TC-TST-06](测试用例.md) | 实现已稳定为 "追加 + Warn",三本文档与测试名残留旧语义 |
| `UIPrefabManager` Facade 模块列表补全 `OwnerRegistry` / `HolderLifecycle` + 设计动机 | [架构设计-v2.md §2.8 / §2.8.1 / §2.8.2](架构设计-v2.md) | v1 草案只列 2 个协作者,实施时拆为 4 个 |
| (前一版审计的 D-02 已不成立) | — | 测试用例.md TC-MX 源文件路径上轮已自行更正为 `TestUIMeshDataX.cs` |

---

## 3. 上一版审计中已过时/不准确的条目(已废止)

| 旧条目 | 实际状况 | 处置 |
|---|---|---|
| ~~TASK-18 shader 未验证~~ | [UIE-AtlasBlit.shader](../Resources/Shader/UIE-AtlasBlit.shader) 已包含 `_MainTex0..7`、`HUD_8_TEX_SLOTS` 分支与 slot 4..7 采样 | **改为**: shader 功能完整,**性能分叉成本未量化**(进入 §4 T-S-03) |
| ~~TASK-21/22 完全未实现~~ | Coverlet 包已引入,GitHub Actions workflow 已就绪 | **改为**: 已配置,**通过率/产物未验证**(进入 §4 T-S-04) |
| ~~D-02 TC-MX 源文件错指~~ | 测试用例.md 已指向 `TestUIMeshDataX.cs` | 废止 |
| ~~TASK-02 = OWASP 安全复核~~ | 实施计划.md 实际 TASK-02 = 补全 `TestUIGeometry` 乱码注释/命名 | 废止;OWASP 不在 v2 工作清单中,如需可另立任务 |
| ~~TASK-12 完全 Done~~ | `MergeBatcher` 曾仅接入 `TestUIPrefabHolder` 的非 Slim 路径;本轮已新增 `HolderBatchRenderer` 并统一 Holder 渲染入口 | T-A-01 已完成;非 Holder demo 脚本仍视为历史/实验入口 |
| ~~TASK-14 完全 Done~~ | `PerfProbe` 曾只有工具类与单元测试;本轮已接入运行路径 marker + PlayMode CSV 测试 | T-D-01 / T-D-02 已完成 |

---

## 4. 补充任务列表 (未完成 / 未验证 / 待验证)

> 编号约定: T-<域>-<序号>。已完成项保留在表中作为审计闭环;未完成项继续进入下一轮工作。
> 域: V=验证, A=架构集成, D=诊断, S=Shader/性能, P=工程化, R=可移植 / 风险记录。

### V 类 · 测试与基线验证 (高优先,阻塞许多 "Done" 的客观依据)

| ID | 标题 | 描述 | 验收 | 关联 |
|---|---|---|---|---|
| **T-V-01** | EditMode 测试基线 | ✅ 已完成: `Assets/Tests/EditMode/TestUIDataRender.asmdef` 已补成真正的 EditMode 程序集(`includePlatforms: ["Editor"]`)；仓库基线使用 `Unity.exe -batchmode -nographics -runTests -testPlatform EditMode -assemblyNames Tests -testResults Logs\EditModeRepoTests.xml` 执行 | XML 已产出: `Logs/EditModeRepoTests.xml`;`Tests.dll` 共 82 项,82/82 通过。`[测试用例.md](测试用例.md)` 中现有 EditMode TC 状态与最近一次跑结果一致 | H-01 已闭环；全量 EditMode 仍会带出 vendored `UnityEditor.UI.EditorTests.dll`,不作为仓库基线口径 |
| **T-V-02** | PlayMode 测试基线 | ✅ 已完成: `Tests.PlayMode` 程序集在 PlayMode runner 下执行 9/9 通过,覆盖 `TestManagedCodeInJob` / `TestMergeJobs` / `TestUseSlimLifecycle` / `TestPerfProbeRuntime` / `TestBurstMergeJobs` / `TestPerfBaselineRunner` | XML 已产出: `Logs/PlayMode_TestsPlayMode.xml`;PlayMode asmdef 已修正为真正 PlayMode 程序集 | TASK-16 / TASK-19 |
| **T-V-03** | CI 工作流真实跑通验证 | 触发 `.github/workflows/unity-tests.yml` 一次,确认 Unity License Activate、EditMode/PlayMode 步骤、Coverlet 产物上传都成功;失败的步骤定位并修复。若当前缺少 `UNITY_LICENSE` 或存在未提交本地包改动,先用 `.\Tools\Invoke-LocalValidation.ps1` 做本地替代验证 | 一条绿色 CI run 链接 + Coverlet artifact 可下载 | TASK-21 / TASK-22 |
| **T-V-04** | Coverage 阈值落实 | 跑 CodeCoverage 包,确认覆盖率报告产出;若 [实施计划.md](实施计划.md) 已写入门禁阈值,核对其在 CI 中实际生效(未通过门禁应使 CI 红) | CI 在低于阈值时 fail,符合实施计划.md 描述 | TASK-21 |

### A 类 · 架构集成 (功能已具,但未渗透到全部路径)

| ID | 标题 | 描述 | 验收 | 关联 |
|---|---|---|---|---|
| **T-A-01** | `MergeBatcher` 统一渲染路径接入 | ✅ 已完成: 新增 `HolderBatchRenderer`;`TestUIPrefabHolder` 不再分 Slim/非 Slim 两套绘制,统一走 `MergeBatcher.Plan` → `RenderBatch` → `MaterialBinder.Bind`。`BatchUIDataRender` 属历史 UIMeshData demo,非 Holder 入口 | `dotnet build` 通过;TC-MB-04/05 覆盖运行时分批与 slot remap | TASK-12 后置 |
| **T-A-02** | 实现层目录迁移收尾 | ✅ 已完成: Runtime 根目录仅保留 `UIDataRender.asmdef` 与子目录;接口契约移入 `Abstraction/`,Mesh 数据移入 `Mesh/`,几何池移入 `Geometry/`,Prefab 管理移入 `Prefab/`,Job 移入 `Jobs/`,工具类移入 `Utility/`;源码与 `.meta` 同步移动以保留 GUID | 根目录无 `.cs` 残留;`dotnet build UIDataRender.csproj` 通过 | [架构设计-v2.md §5](架构设计-v2.md) 目标目录 |
| **T-A-03** | EditMode asmdef 物理归位 | ✅ 已完成: `Assets/Tests/TestUIDataRender.asmdef` 已移入 `Assets/Tests/EditMode/TestUIDataRender.asmdef`,与 PlayMode asmdef 对称;`.meta` 同步移动 | asmdef 在 `EditMode/` 子目录;`dotnet build Tests.csproj` 通过 | TASK-16 后置 / 低优 |
| **T-A-04** | Burst-friendly SoA Job 实现 | ✅ 代码/等价性已完成: 新增 `BurstMergeJob` + `MeshSlimSoA`,以 `NativeArray` SoA 输入替代 `UIMeshData[]` 托管对象;保留 `ManagedCodeInJob` 兼容路径 | `dotnet build` 通过;TC-JOB-03 覆盖输出等价。性能下降幅度仍需 T-S/T-V 基线量化 | TASK-19 |

### D 类 · 诊断与可观测性

| ID | 标题 | 描述 | 验收 | 关联 |
|---|---|---|---|---|
| **T-D-01** | `PerfProbe` 运行路径埋点接入确认 | ✅ 已完成: `DataPrefabHolder.Fill`、`HolderBatchRenderer` 的 Merge/Fill/Draw 路径已接入 `ProfilerMarker.Auto()`;运行时 rebuild 会写入 `PerfProbe.Record` | TC-MB-04 与 TC-PP-03 覆盖 `probe.Count > 0` | TASK-14 后置 |
| **T-D-02** | CSV 落盘自动化验证 | ✅ 已完成: 新增 PlayMode 用例,运行时 rebuild 后 Flush 并校验 CSV | TC-PP-03 | TASK-14 |

### S 类 · Shader 与性能

| ID | 标题 | 描述 | 验收 | 关联 |
|---|---|---|---|---|
| **T-S-03** | 8-slot 着色器分叉性能量化 | Shader 功能已完整,但 `HUD_8_TEX_SLOTS` 关键字开/关两版的 GPU 成本(VRAM、采样器、寄存器压力)未量化。需在目标机型(Android + Editor)上跑 Profiler / RenderDoc 截帧对比 | 一份基线对比报告(开 vs 关 8-slot)放入 [Read.MD](Read.MD) 或新建附录 | TASK-18 后置 |

### P 类 · 工程化

| ID | 标题 | 描述 | 验收 | 关联 |
|---|---|---|---|---|
| **T-P-01** | 审计文档纳入版本控制 | 当前 `Assets/Doc/实施状态审计.md` 与 `.meta` 均为 untracked。需 `git add Assets/Doc/实施状态审计.md*` 并提交 | git status 干净 | 工程卫生 |
| **T-P-02** | License 通道协议升级 | License Client 报 `protocol 1.16.1 unsupported (max 1.10)`。需升级 Unity Hub / Unity Editor 或回滚 LicensingClient 至兼容协议,使 batchmode 测试可在 Editor 打开时也能跑(目前必须关闭 Editor) | Editor 打开时 batchmode 测试可并行 | 工程卫生 |

### R 类 · 风险记录(低优,无需立刻动手)

| ID | 标题 | 描述 |
|---|---|---|
| **R-01** | `UIMeshDataX.NEXT` 跨测试不归零 | 静态 `int NEXT`([UIMeshDataX.cs](../Scripts/UIDataRender/Mesh/UIMeshDataX.cs) L23) 仅用于诊断日志;当前测试不依赖其值。若未来有断言依赖,需移到实例或在 `Reset` 中归零 |

---

## 5. 优先级建议

1. **立刻**: T-V-03 → T-V-04(继续拿到 CI 与覆盖率的客观基线,"Done" 才算结案)
2. **本轮已完成**: T-V-01(EditMode Unity Test Runner 仓库基线)、T-A-01(MergeBatcher Holder 渲染入口接入)、T-D-01(PerfProbe 埋点接入)、T-D-02(PlayMode CSV 验证)
3. **下一轮工作**: T-V-03(CI 工作流真实跑通验证)；可延后 T-S-03(性能量化)、R-01

---

## 6. 场景级测试补充(本轮新增)

新增 [场景测试说明.md](场景测试说明.md):
- 文档化 10 个现有 `Assets/Scenes/*.unity` 的测试意图、操作步骤、关联 TC 与 v2 模块。
- 设计 **9 个待建场景**(`📋` 标记),按域分组:
  - **多纹理域** (T-S-A-01..04): 覆盖满槽、超容量分批 + Warn、`HUD_8_TEX_SLOTS` 8-slot keyword、动态 Swap。
  - **性能基线域** (T-PB-01..05): UGUI / UIMeshData / UIMeshDataX / +Job / 高并发压测的 A/B/C/D 矩阵。
  - **端到端诊断域** (T-D-S-01): `PerfProbe.Flush` CSV 落盘的场景级演示。
- 与本审计任务交叉: T-S-A-02 落地 TC-TST-06 场景验证;T-S-A-03 为 T-S-03 8-slot 性能量化提供 setup;T-PB-* 为 T-A-04(Burst SoA)与 T-V-* 基线测试提供数据源;T-D-S-01 为 T-D-02 提供可视化端到端验证。

> 新场景应在 Unity Editor 中搭建并将 `.unity` + `.meta` 一并提交;勿用文本/命令行生成 `.unity` 文件。

---

## 7. 与第二位 agent 的事实差异说明

- 第二位 agent 报告 "`UIDataRender.csproj` 与 `Tests.csproj` 通过 `dotnet build`",这只能证明 C# 编译通过,**不能替代 Unity Test Runner 结果**。Unity 测试需要 Unity Editor 进程加载场景与 asmdef,`dotnet build` 路径不会跑 NUnit 测试用例。两者结合解读:**编译已绿,行为未验证**。
